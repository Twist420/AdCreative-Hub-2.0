# Vue 3 + JS 1:1 迁移差异清单

更新日期：2026-06-28

## 当前口径

React 原项目是金标准。Vue 3 + JS 版本不能只做到页面可运行，还需要对齐。当前用户明确口径：除“人员效能表”外，其它页面都按 1:1 迁移验收。

- 页面信息结构、字段顺序、控件密度
- 筛选、排序、分页、列配置、多选、弹窗等交互
- 详情、上传、移动、复制、状态计算等业务动作
- 视觉层级、圆角、阴影、表格宽度、空态和提示

## 本轮已补

- 回收数据：恢复 Campaign / Set / 素材名称的多选搜索筛选。
- 回收数据、消耗数据：恢复表头指标说明浮层。
- 规范画布：补回 Controls / Ratios 的按钮、标签、编辑态、媒体比例和模糊背板说明。
- 规范画布：补回 Snippets 代码块和 Copy Hub 结构。
- 资产库：补回上传素材、批量选择、批量移动、目录属性控制、管理员强制删除的主流程。
- 资产库：上传素材按目标目录 prefix / defaultTags 生成编号与标签。
- 资产库：补回新建空资产库后的配置并激活 setup flow，以及上传区拖拽添加附件状态。
- 资产库：补回父级目录下的右侧子文件夹网格、资产数量、四宫格预览和目录管理入口。
- 资产库：补回移动弹窗内新建目的资产库并移入资产，以及目录强制删除后回到全部资产的原版口径。
- 资产库：补回上传/移动/目录配置/管理员删除的原版校验与成功反馈，包括空名称、未选目录、无有效素材名、管理员密码错误、上传成功、批量移动成功等 toast 提示，并对齐高危删除和移动归属说明文案。
- 总览看板：补回 Campaign / Set 自绘下拉、Top20 花费结构图表组、三组渠道堆叠图、CPA7 图表组、legend 显隐、堆叠模式切换、日期轴标签、hover 指标浮层和语言/素材类型多选筛选口径。
- Benchmark：补回按渠道和平台的生效状态计算。
- 规范画布：修复布局沙盒 Vue ref 绑定，补回当前沙盒 class 复制入口，并把 Snippets 内残留 React JSX 片段翻成 Vue 模板写法。
- 标签管理：补回标签 pill 拖拽到标签组的迁移操作，删除确认改为自绘确认浮层。
- 标签管理：补齐 React 原版标签全集，包括角色形象下级标签、角色性别/年龄层、玩法体验、文案、形式、落板、节日/活动和创意标签缺项；布局恢复为原版搜索工具栏、二级分区纵向、三级标签组卡片网格和隐藏标签组折叠收纳。
- 需求中心：删除排期、删除需求统一改为自绘确认弹窗，移除原生 confirm 残留。
- 需求中心：制作排期容量视图、甘特视图、日历筛选、详情排期面板和方向卡编辑菜单补齐头像+名字展示。
- 需求详情：补回 React 原版空 subVersions 时的 5 个默认小版本，避免点开卡片后成片、脚本工作台、小版本复制列表信息量少于原版。
- 全局布局：侧边栏恢复 React 原版 44px 折叠宽度、拖拽调整宽度、localStorage 记忆宽度/折叠状态、折叠 tooltip、14px 顶部高度和总览图标口径。
- 规范画布：补回 hero 渐变与氛围光、完整 token 名称/说明、Typography 原版文案、Controls 标题图标、卡片边框层级等 1:1 可见差异。
- 规范画布：继续贴回空间沉淀法案、色彩分组、复制成功提示、Controls 徽章/按钮/表单/标签编辑器、Ratios 媒体比例和 Copy Hub 说明原文。
- 资产库：补回筛选状态条“当前查看/清除筛选”、已选标签清除、排序方式原版标签、空态说明、多行上传提示、移动弹窗选择已有目的库文案和分类层级原文。
- 资产库：补回“从该资产创建迭代资产”交互，生成迭代版资产、清空表现数据、引用数归零、切回来源目录并打开新资产详情。
- 总览看板：继续补齐 Top20 素材花费结构与类型占比标题、渠道切换副标题和堆叠模式控件标签。
- 消耗数据：素材类型筛选 placeholder 恢复为 React 原版“素材类型”。
- Benchmark：新建规则弹窗字段名补齐“生效时间 (北京时间)”“付费用户数 (Int)”“新增用户数 (回收标准)”。
- 迭代记录：画布操作提示恢复为“拖拽移动，滚轮缩放”。
- 需求详情：复制、保存、退出 toast 恢复 React 原版文案；小版本查看按钮补回“点击查看并复制所有小版本名称”提示；成片上传按钮补回“上传当前需求的成片素材”提示；排期项上移、下移、删除按钮补回原版 title。
- 需求脚本：富文本编辑器默认占位恢复 React 原版脚本结构模板，包括叙事结构、开头钩子、用户核心痛点、玩法、场景和人设化内容说明。
- 总览看板：恢复 React 原版固定 KPI 展示值、明细文案、overview mock 数据生成算法、语言/渠道折算、总花费独立随机种子和 Top20 固定素材编号序列。
- 总览看板：补回 Top20 堆叠图和 CPA7 图表的分页 legend 交互，包括上一页/下一页、禁用态、每页 6 个素材切换。
- 资产库：侧边栏标题恢复为“资产库目录 / Library Explorer”，全部资产按钮补回“点击返回最上层级”提示，新建目录输入占位恢复“子分类名称”。
- 资产库：批量选择操作条恢复原版图标、英文状态说明、移动按钮和“取消操作”按钮，并修复移动成功 toast 清空选择后数量变成 0 的问题。
- 规范画布：布局沙盒补回右侧“实时模拟”状态标识。
- 需求脚本工作台：确认 Vue 版已按模块拆到 `requirement-detail/RequirementScriptWorkbench.vue` 与 `scriptWorkbenchData.js` 等子组件，并补齐模板类型、资产/成片/落版引用、目录树、筛选、自由模板矩阵、CTA/落版和上传参考的原版文案口径。
- 需求脚本工作台：补回简单素材验证目标字段标签、Playable/Image 描述占位、布局模式标识、目录展开/收起/选择 title，以及自由模板默认“自动引用原始成片作为本地化参考。”说明。
- 需求中心：本地化新建需求禁用原因恢复 React 原版顺序和文案，先提示“请至少选择 1 条来源需求”，再提示“请至少选择 1 个本地化语言”。
- 需求详情：右侧迭代记录默认来源恢复 React 原版“迭代自: cp3632-01”；制作排期负责人自绘菜单里的推荐组名恢复为“组名（推荐）”口径。
- 隐藏组件：补回 React 母版中存在但当前导航未挂载的 `CreativeAnalysis.tsx`，翻译为 `vue3-js-app/src/components/CreativeAnalysis.vue`，并补齐 `creative-analysis/creativeAnalysisParts.js` 与 mock 数据生成函数。
- 隐藏组件：补回 React 母版中存在但当前导航未挂载的 `MaterialDetails.tsx`，翻译为 `vue3-js-app/src/components/MaterialDetails.vue`，包含双日期筛选、搜索、字段配置、条件筛选、排序、空态和底部分页占位；条件字段与操作符下拉按 Vue 项目规则使用自绘 `AnalyticsSelect`。
- 编译兜底：`App.vue` 对 `CreativeAnalysis.vue` 和 `MaterialDetails.vue` 做隐藏编译引用，但不新增导航入口，保持 React 当前可见入口一致。
- 资产库：详情承载从 Vue 临时右侧抽屉恢复为 React 原版居中大弹窗 `DetailModal.vue`，补回阅读/编辑切换、状态编辑、比例切换、预览区域、固定属性、标签编辑、渠道表现、关联创意、关联需求、父资产/引用资产、迭代入口和保存写回。
- 资产库：详情弹窗补回 React 原版父资产/引用资产关系选择器深层交互，包含目录树筛选、全部资产入口、搜索、父资产单选、引用资产多选/取消、已选资产卡片和空态。
- 资产库：详情弹窗继续贴回原版细节文案和编辑提示，包括“打开源文件 / 未配置源文件”、无源文件提示、资产标题占位、主题占位、快捷标签组、“真机实测”、移除关联需求 title、“添加引用资产”和“清空父资产 / 移除引用资产”等操作提示。
- 资产库：详情弹窗继续对齐 React 原版信息结构，补回左侧入库时间/源文件地址小卡、右侧“核心获客数据统计指标”8 列渠道表、概览页“资产结构关系”表，以及“关联需求”页签里的投放汇总和 7 列需求投放表现表。
- 资产库：关系选择器提升为详情弹窗公共覆盖层，概览页资产结构关系按钮和关联需求页均可打开，避免选择器只挂在某个页签里导致按钮无反馈。
- 资产库：详情弹窗补回原版编辑底部工具条、“适用位置”标签、关系选择器已选状态、完成选择按钮和无匹配资产二级说明。
- 资产库：清理 `DetailModal.vue` 中被替换后暂时隐藏的旧关系块，避免同一关系编辑能力在源码里重复维护。
- 资产库：补齐新建/移动/删除相关原版文案，包括“资产库已成功配置并激活！”，“➕ 新建目的资产库并移入资产”，“🔒 解锁删除此资产库”，“💥 确认强制删除”，以及新建资产库弹窗的“例如: 智能AI解说, 冰原大厅”和“完成并生成资产库”。
- 规范画布：补回布局沙盒和 Snippets 的原版符号标题，包括演示主题配色、内边距比例、底层投影融合、标准极简卡片容器、读写态控制器、汇总图表表头和轻量弹窗遮罩。
- 协同看板：方向卡编辑态补回“输入有效个数 / 输入总计划数”title；删除排期及旧排期行统一走自绘确认弹窗并恢复 React 原版“确定删除此排期及方向？”、“确定删除此行？”文案。
- 标签管理：删除标签确认浮层标题/正文恢复 React 原版“删除标签 / 确定删除吗？”。
- CreativeAnalysis：补回“点击查看素材明细”hover 提示，并保持表格“计数占比”原版表头口径。
- 总览看板：KPI 明细字符串间隔改为源码单空格形式，减少 React/Vue 静态对照误差，视觉展示保持一致。
- 素材上传：上传投放区标题恢复 React 原版“点击或分发文件到此区域上传”文案。
- 资产库：排序方式恢复 React 原版字段口径，补回“引用 / 预算 / 安装 / IR / CPI / 更新时间 / 视频长度”，并按花费、安装、IR、CPI 等 performance 指标排序；CPI 保持原版低值优先规则。
- 资产库：左侧目录树改为独立递归 Vue 组件并恢复完整目录渲染，补回原版行内新建子资产库输入，含“子分类名称”占位、确认/取消按钮、Enter 提交和 Escape 取消。
- 协同看板：通用筛选条件删除按钮补回“删除筛选条件”可访问标签，方向详情标签添加按钮补回“添加方向标签”title。
- 协同看板：制作流程工具函数补回 React 原版非 3D 视频阶段“2D视频”命名。
- CreativeAnalysis：指标配置补回 tooltip 二级标签口径“花费占比 / 数量占比”，为后续继续补图表 hover 细节保留原版文案。
- 服务层：`mockData.js` 补齐 React 原版 `summarizeRequirementFeedback` 导出，保持成片投放反馈的需求级汇总接口一致。
- 服务层：补回 Vue 版 `geminiService.js` 的 `analyzeMaterials` 接口，Overview 的“Refresh AI Analysis”恢复走服务层；无 API key 时按原版回落到 mock 分析数据，并移除 Vue 临时多出的关键词列表展示。
- 共享工具：补回 React 原版 `shared/date/dateRange` 对应的 Vue/JS 共享日期 helper，并让 creative-analysis 继续通过统一入口复用 `getRecentUtcRange`。
- 共享工具：补回 React 原版 `shared/requirements/requirementId` 对应的 Vue/JS 需求编号 helper，并让 `requirementUtils.js` 通过 shared helper re-export，避免需求编号解析/展示逻辑分叉。
- 全局常量 / mock 数据：补回 `TEST_DIRECTIONS` 常量，并将 Vue `mockKeywordAnalysis` 从临时 `keywords` 结构恢复为 React 原版 `positiveTags / negativeTags / summary` 结构。
- 需求中心：本地化新建需求补回 React 原版成片表现数据链路，`recentSpendMap` 改为近 30 天投放花费汇总，来源需求排除派生本地化需求，并把 `finishedCreativePerformance` 传入小版本引用生成逻辑。
- 浏览器冒烟：刷新本地 Vue 页面后确认需求中心无白屏/无 Vite error overlay；切到资产库后确认排序字段、完整目录树、21 个“新建子资产库”入口和行内“子分类名称”输入可出现并可 Escape 取消。
- 协同看板：排期与需求 mock 数据生成逻辑对齐 React 原版，包括 10 组周期、方向难度/类型、结转状态、方向标签、`sched-12` 多版本需求、`sched-14` 已完成未投放等口径；避免 Vue 版内容看起来像旧样例。
- 协同看板：方向卡底部需求编号从静态标签恢复为可点击需求入口，数据对齐后点击 `cp3359` 可直接打开需求详情弹窗，并显示父文件夹、小版本、需求脚本、成片等关键区域；避免此前点击编号无响应/只停留在方向卡的问题。
- 浏览器冒烟：主导航页面、数据分析子页、需求中心四个子页均完成点击检查，无白屏、无 Vite error overlay、无 console error。
- 需求中心工具层：补齐 Vue `requirementFactory.js` 对 React 原版 `getRequirementIdPrefix` 的导出，并为 `LOCALIZATION_LANGUAGES` 保留原版大写导出名，同时兼容当前小写引用。
- 需求详情工具层：补回 React 原版 `PERSON_AVATAR_URLS`、`PRODUCTION_PEOPLE`、`formatScheduledRequirementId`、`getRequirementMajorId`、`parseRequirementVersionId` 的导出面；制作人员推荐组改为复用导出的 `PRODUCTION_PEOPLE`。
- 需求详情工具层：`DIMENSIONS_LIST` 移除 Vue 临时多出的 `4:5` 项，恢复 React 原版 `9:16 / 1:1 / 16:9` 三项口径。
- 静态对照：完成组件文件图谱、服务层导出、需求中心工具导出和需求详情工具导出检查；除人员效能和 TS 类型接口外，当前未发现会导致 Vue 端按原模块习惯 import 缺失的运行时导出。
- Benchmark：新建规则弹窗恢复 React 原版数值字段分组布局，表头数值列/操作列对齐恢复原版居中/右对齐，并把 ARPPU7 空值显示恢复为 `$—`。
- 迭代记录：Vue 版从临时递归 DOM 树改回 React 原版 D3/SVG 画布实现，恢复横向 tree layout、D3 拖拽/滚轮缩放、节点右上角添加子节点、根节点预览播放和非根节点视频列表点击播放。
- 迭代记录：Vue 子项目补充 `d3` 依赖并更新 `package-lock.json`，保证迁移版独立安装后也能运行 D3 画布。
- 本地预览：Vite dev server 当前运行在 `http://127.0.0.1:5181/`；HTTP 入口返回 200。内置浏览器插件本轮两次连接超时，未完成截图级验收。
- 数据分析：总览看板、回收数据、消耗数据移除 Vue 临时固定 `2026-06-27` 的最近 30 天函数，统一改回 React 原版共享 `getRecentUtcRange(30)` 口径；固定日期残留仅剩已排除严格 1:1 的人员效能页。
- 静态对照：补查 UiSpecification、MaterialUpload、RecoveryData、ConsumptionData 的中文文案覆盖，当前未发现母版文案缺失；资产库主文件文案误报已在 Vue 子组件中确认存在。
- 标签管理：补回 React 原版拖拽标签时的容器边缘自动滚动，靠近上下 96px 区域按原版速度自动滚动，并在 dragend/drop/组件卸载时停止动画帧。
- 静态对照：补查协同看板、排期选择弹窗、排期详情、旧排期表、标签管理和需求详情拆分模块；当前剩余中文缺口主要为 React 注释、Vue 子组件承载或多行模板折叠误报。
- CreativeAnalysis：可视化分析从 Vue 临时手写柱形继续对齐 React 原版 Recharts 语义，补回图表网格、平均花费虚线与数值标注、hover 指标浮层、点击查看素材明细提示；数据表补回平均花费差异条、花费/计数/占比条形底色和“大盘总计”减号标识。
- CreativeAnalysis：详情下钻弹窗素材行补回日期图标和右侧 7 日花费趋势小图，恢复 React 原版素材行的信息密度和趋势入口。
- 静态对照：补查资产库主页面、资产详情弹窗、资产卡、资产数据 helper、需求详情、需求脚本工作台、排期可用性弹窗、上传成片弹窗、小版本弹窗和 MaterialDetails；除 React 注释、正则片段和子组件/变量拼接误报外，当前未发现中文文案缺口。
- 协同看板：旧排期表 `LegacyScheduleTable.vue` 的需求截止/制作截止日期输入补回 React 原版 `showPicker` 点击打开原生日期选择器行为。
- 数据分析：补查消耗/回收字段配置下拉，Vue 版已包含 React 原版字段显隐、拖拽排序、外部点击关闭和 `Escape` 关闭能力。
- 需求中心工具层：`dateUtils.js` 补回 React 原版 `ProductionSubmitDateDisplay`、`WeekRangeRuleInfo` 和 `openNativeDatePicker` 导出面，前两个以 Vue 单文件组件 re-export。
- 需求详情工具层：`detailUtils.js` 补回 React 原版 `openNativeDatePicker` 导出；剩余 `Dropdown` / `InlineInput` 等 React JSX 小控件已由 Vue 模板/组件承载，不塞回工具 JS。
- 数据分析：`DateRangePicker.vue` 增加分析模式，Overview / CreativeAnalysis / ConsumptionData / RecoveryData / MaterialDetails 的双日期筛选恢复 React 原版 `AnalyticsDateRangeField` 的 UTC 口径、`Last 30 days / 投放时间` 标签、fixed 浮层、快捷日期、月份/年份自绘下拉和范围选中行为；需求中心继续保留 React 通用 `DateRangePicker` 口径。
- 资产库：删除 Vue 迁移早期遗留且未引用的 `asset-library/AssetDetailDrawer.vue`，避免与已恢复 React 原版居中大弹窗 `DetailModal.vue` 并存造成交付误用；删除后 Vue build/lint 均通过。
- 服务层：`generateTopMaterials` 恢复 React 原版随机投放日期、失败素材编号和 thumbnail seed 算法；`generateMaterialDetails` 素材标题尺寸恢复原版 `9×16` 符号，避免总览 Top 素材和素材明细/下钻内容与母版数据不一致。
- 标签管理：初始标签树恢复 React 原版 `normalizeTagTree / flattenTags` 处理，角色形象下的真人角色、卡通人物、动物角色等 5 级子标签不再被 Vue 版拍平成同级；新建标签组默认类型也恢复为原版 `none`。
- Benchmark：状态计算恢复 React 原版每分钟刷新节奏，避免 Vue computed 只在数据变化时更新，导致规则到达生效时间后状态不自动切换。

## 仍需继续 1:1 细修

- 人员效能：已按当前口径排除出严格 1:1 范围；后续只保证可运行和基础视觉一致。
- 全页面：仍需浏览器逐页视觉走查，对齐间距、容器高度、弹窗宽度和移动端溢出。本轮内置浏览器已可连到本地 Vue 页面并完成需求中心/资产库冒烟，但尚未逐页截图级对照。
- React 母版中仍需确认的深层细节：资产详情大弹窗已补回关系选择器主流程、主要编辑文案、性能表、资产结构表和关联需求投放表，并清理旧隐藏关系块；后续主要剩浏览器逐页视觉验收和关键弹窗交互复核。
- 当前静态雷达剩余项主要是需求详情脚本默认模板换行折叠、资产库/CreativeAnalysis 子组件承载、以及 React 注释类误报；Vue 端保留同内容或已拆分到子组件中。
- 运行时检查：本轮 Vue 子项目 `npm run build` 与 `npm run lint` 均已通过，根项目 `npm run lint` 也通过；标签管理和 Benchmark 追加修复后再次运行 Vue 子项目 `npm run lint` / `npm run build` 通过。此前 `http://127.0.0.1:5175/` 曾通过提权 HTTP 检查返回 200 和 Vue 入口 HTML。本轮再次复查 HTTP 入口时提权请求因额度限制被拒，未绕路重试；内置浏览器插件此前打开本地页仍超时重置，截图级/DOM 级验收仍需后续恢复浏览器工具后继续。
